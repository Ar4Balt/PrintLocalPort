Конечно! Вот пример **красивого и читаемого README** для GitHub, полностью описывающего твой скрипт:

```markdown
# Printer Ports Manager (Windows)

**Printer Ports Manager** — это удобный скрипт на Windows Batch (`.bat`), который позволяет легко управлять сетевыми портами принтеров на ПК. Он позволяет **добавлять, удалять и просматривать порты**, автоматически подставляя имя текущего компьютера.

---

## 🎯 Возможности
- Просмотр всех существующих портов в системе.
- Добавление предустановленных сетевых портов или ввод собственного UNC пути.
- Удаление ненужных портов по номеру или вручную.
- Проверка на существование порта перед добавлением.
- Автоматический перезапуск службы печати после внесения изменений.
- Поддержка нескольких портов одновременно (выбор через запятую).

---

## ⚙️ Как это работает
1. Скрипт автоматически получает имя текущего компьютера (`%COMPUTERNAME%`) и подставляет его в пути к портам.
2. В меню пользователь может выбрать:
   - Добавить порты
   - Удалить порты
   - Выйти
3. При добавлении:
   - Можно выбрать один или несколько предустановленных портов.
   - Можно ввести собственный порт вручную.
   - Скрипт проверяет, есть ли порт уже в системе.
4. При удалении:
   - Можно выбрать номера портов из списка.
   - Можно ввести полный путь порта вручную.
5. После всех изменений автоматически перезапускается служба печати, чтобы изменения вступили в силу.

---

## 📋 Предустановленные порты
Скрипт содержит следующие сетевые порты:

- `KIP7170`
- `OCE-360 PW360 WPD2`
- `OCE-360.AZOT.COM.BY_PW360_WPD2`
- `PRN-OKI-2BCB2D`
- `PRN-EPS-AED7D2`
- `PRN-PAN-49509E`
- `PRN-PAN-4954AE`
- `PRN-PAN-32DC63`
- `PRN-PAN-DF6D5A`
- `PRN-PAN-54BCA6`
- `PRN-PAN-32DBFA`
- `PRN-PAN-54BCAD`
- `PRN-PAN-374302`
- `PRN-XER-9222E4`
- `TCS300_1_TCS300_WPD2`
- `TCS300_2_TCS300 WPD2`
- `HPOA403D`
- `HPOA91AD`
- `HPOA411D`
- `HP0A411D`
- `XEROX-6705`

> Все они автоматически подставляются с именем текущего ПК: `\\%COMPUTERNAME%\PORT_NAME`.

---

## 💻 Требования
- Windows 7/8/10/11
- Права администратора для изменения HKLM и перезапуска службы печати

---

## 🚀 Использование
1. Скачайте скрипт `printer_ports_manager.bat`.
2. Запустите его от имени администратора.
3. Выберите действие в главном меню:
   - **1** — Добавить порты
   - **2** — Удалить порты
   - **3** — Выйти
4. Следуйте инструкциям на экране:
   - Выберите номера портов через запятую или введите вручную.
5. После каждой операции служба печати перезапускается автоматически.

---

## 🔧 Примечания
- Скрипт управляет портами в реестре Windows по пути:
```

HKEY\_LOCAL\_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Ports

```
- Проверяет существование порта перед добавлением, чтобы избежать дубликатов.
- Удаление портов можно делать по номеру или указав полный UNC путь.

---

## 📂 Структура проекта
```

printer\_ports\_manager/
│
├─ printer\_ports\_manager.bat   # Главный скрипт
└─ README.md                  # Документация

```

---

## ⚠️ Внимание
- Обязательно запускайте скрипт **от имени администратора**, иначе добавление и удаление портов в HKLM не будут работать.
- Скрипт перезапускает службу печати, что может временно прервать задания печати.

---

## 📬 Обратная связь
Если у вас есть идеи по улучшению скрипта или найден баг — создавайте **issue** в этом репозитории.

---

> Сделано с ❤️ для упрощения управления сетевыми портами на Windows.
```

---

Если хочешь, я могу сделать ещё **версию README с красивым современным стилем**, с **иконками, цветными блоками и разделами “Примеры использования”**, чтобы прям на GitHub выглядело как продукт 2025 года.

Хочешь, чтобы я так сделал?
